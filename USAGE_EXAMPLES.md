# 도매꾹 검색 도구 - 실행 방법 가이드

## 📋 목차
1. [기본 실행 방법](#기본-실행-방법)
2. [인기 키워드 자동 수집](#인기-키워드-자동-수집)
3. [실전 사용 예시](#실전-사용-예시)

---

## 기본 실행 방법

### 1. 대화형 모드 (기본)

```bash
python main.py
```

실행 후 검색어를 입력하라는 프롬프트가 나타납니다:
```
검색어: 양말
```

여러 검색어를 쉼표로 구분하여 입력할 수 있습니다:
```
검색어: 양말, 장갑, 모자
```

### 2. 빠른 검색 모드

```bash
# 단일 검색어
python main.py -q 양말

# 여러 검색어
python main.py -q 양말 장갑 모자
```

### 3. 파일에서 검색어 읽기

```bash
# keywords.txt 파일 생성
echo "양말" > keywords.txt
echo "장갑" >> keywords.txt
echo "모자" >> keywords.txt

# 실행
python main.py --file keywords.txt
```

---

## 인기 키워드 자동 수집

### 기본 사용

```bash
# 자동 모드 (10개 키워드)
python main.py --trending
```

### 네이버 쇼핑에서 수집

```bash
# 네이버 쇼핑 베스트 페이지에서 15개 키워드 수집
python main.py --trending --trending-source naver --trending-count 15
```

**수집 기준:**
- 네이버 쇼핑 베스트 페이지의 상품명
- 카테고리별 인기 상품명

### 쿠팡에서 수집

```bash
# 쿠팡 베스트셀러에서 20개 키워드 수집
python main.py --trending --trending-source coupang --trending-count 20
```

**수집 기준:**
- 쿠팡 베스트셀러 페이지의 상품명
- 카테고리별 베스트 상품명

### 아이템 스카우트에서 수집

```bash
# 아이템 스카우트 트렌드에서 10개 키워드 수집
python main.py --trending --trending-source itemscout --trending-count 10
```

**수집 기준:**
- 아이템 스카우트 트렌드 페이지의 키워드
- 인기 키워드 분석 데이터

### 과거 검색 결과에서 추출

```bash
# result 폴더의 JSON 파일에서 자주 나오는 키워드 추출
python main.py --trending --trending-source results --trending-count 15
```

**수집 기준:**
- `result/` 폴더의 모든 JSON 파일 분석
- 상품명에서 키워드 추출 및 빈도수 계산

---

## 실전 사용 예시

### 예시 1: 네이버 쇼핑 인기 키워드로 검색

```bash
python main.py --trending --trending-source naver --trending-count 10 --pages 2
```

**동작:**
1. 네이버 쇼핑에서 10개 인기 키워드 수집
2. 각 키워드로 최대 2페이지까지 검색
3. 결과를 JSON 파일로 저장

### 예시 2: 쿠팡 베스트셀러 키워드 + 가격 필터

```bash
python main.py --trending --trending-source coupang --trending-count 5 --min-price 15000 --max-results 20
```

**동작:**
1. 쿠팡에서 5개 인기 키워드 수집
2. 각 키워드로 검색 (15,000원 이상만)
3. 키워드당 최대 20개 결과

### 예시 3: 인기 키워드 검색만 (마이박스 추가 안 함)

```bash
python main.py --trending --trending-source naver --trending-count 10 --no-mybox --format csv
```

**동작:**
1. 네이버 쇼핑에서 10개 키워드 수집
2. 검색만 수행 (마이박스 추가 안 함)
3. 결과를 CSV 형식으로 저장

### 예시 4: 헤드리스 모드로 자동화

```bash
python main.py --trending --trending-source auto --trending-count 15 --headless --no-mybox
```

**동작:**
1. 자동으로 여러 소스에서 키워드 수집 시도
2. 브라우저 창 숨김 (헤드리스 모드)
3. 검색만 수행

### 예시 5: 과거 결과 분석 후 재검색

```bash
# 1단계: 과거 결과에서 인기 키워드 추출
python main.py --trending --trending-source results --trending-count 10 --no-mybox

# 2단계: 추출된 키워드로 재검색 (더 많은 페이지)
python main.py --trending --trending-source results --trending-count 10 --pages 5
```

---

## 옵션 조합 예시

### 인기 키워드 + 여러 페이지 + 가격 필터

```bash
python main.py --trending \
  --trending-source naver \
  --trending-count 10 \
  --pages 3 \
  --min-price 12000 \
  --max-results 50
```

### 인기 키워드 + 상세 출력 + 로그 파일

```bash
python main.py --trending \
  --trending-source coupang \
  --trending-count 5 \
  --verbose \
  --log-file logs/trending.log \
  --log-level DEBUG
```

### 인기 키워드 + CSV 저장 + 헤드리스

```bash
python main.py --trending \
  --trending-source itemscout \
  --trending-count 20 \
  --format csv \
  --headless \
  --no-mybox
```

---

## 💡 팁

1. **처음 사용 시**: `--trending-source auto`로 시작하여 어떤 소스가 가장 잘 작동하는지 확인
2. **빠른 테스트**: `--trending-count 3`으로 작은 수부터 시작
3. **대량 수집**: `--headless --no-mybox`로 빠르게 검색만 수행
4. **결과 분석**: `--trending-source results`로 과거 데이터에서 패턴 발견

---

## ⚠️ 주의사항

1. **이용약관 준수**: 각 사이트의 이용약관을 확인하고 준수하세요
2. **접근 제한**: 과도한 요청 시 IP 차단될 수 있습니다
3. **로그인 필요**: 아이템 스카우트는 로그인이 필요할 수 있습니다
4. **페이지 구조 변경**: 사이트 업데이트 시 선택자 수정이 필요할 수 있습니다

